{
  "name": "amzhelper",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd server && npm install && cd ../app && npm install && npm run build",
    "start": "node server/server.js",
    "dev": "cd app && npm run dev"
  }
}
